{
  "name": "gaze-run-interrupt",
  "version": "1.0.1",
  "description": "Watch files with gaze and run sequence of commands. Interrupts previous sequence if not finished.",
  "main": "dist/src/index.js",
  "files": [
    "src",
    "dist"
  ],
  "repository": "bytearcher/gaze-run-interrupt",
  "bugs": "https://github.com/bytearcher/gaze-run-interrupt/issues",
  "dependencies": {
    "chalk": "^1.1.3",
    "gaze": "^1.0.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "rimraf": "^2.5.2",
    "typescript": "^1.8.10",
    "typings": "^1.0.4"
  },
  "scripts": {
    "prepublish": "typings install && npm run build",
    "clean": "rimraf dist",
    "build": "tsc",
    "test": "cd dist ; mocha"
  }
}
